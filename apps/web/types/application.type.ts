export type Application = {
  creator?: string;
  desktopIconUrl: string;
  disabled: boolean;
  fullScreen: boolean;
  iconUrl: string;
  minimized: boolean;
  name: string;
  system?: boolean;
  zIndex: number;
};
