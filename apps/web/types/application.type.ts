export type Application = {
  creator?: string;
  desktopIconUrl: string;
  disabled: boolean;
  fullScreen: boolean;
  iconUrl: string;
  id: string;
  minimized: boolean;
  name: string;
  system?: boolean;
  zIndex: number;
};
